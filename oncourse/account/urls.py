from django.conf.urls import patterns, url

urlpatterns = patterns('oncourse.account.views',

    # Account URLs
    # url(r'^$', 'index', name='settings-index'),
    # url(r'^account/$', 'account', name='settings-account'),

)
